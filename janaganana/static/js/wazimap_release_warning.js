$(function() {
  // add banner on head2head pages
  var banner = $('<div id="banner"><div class="wrapper centered">Be careful when comparing numbers from different releases. Ward data is currently only available for 2011.</div></div>');
  $('#page-header').append(banner);
});
